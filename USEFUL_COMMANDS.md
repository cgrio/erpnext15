#!/bin/bash
# Comandos Úteis para Gerenciar ERPNext no Portainer
# Copie e cole conforme necessário

echo "╔═══════════════════════════════════════════════════════════════╗"
echo "║  ERPNext Useful Commands                                      ║"
echo "║  Commands para troubleshoot e admin do ambiente              ║"
echo "╚═══════════════════════════════════════════════════════════════╝"
echo ""

# ========== MONITORAMENTO ==========
echo "🔍 MONITORAMENTO E STATUS"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "# Ver todos os containers"
echo "docker ps -a | grep erpnext"
echo ""
echo "# Ver Status detalhado"
echo "docker ps --format 'table {{.Names}}\t{{.Status}}' | grep erpnext"
echo ""
echo "# Ver logs em tempo real"
echo "docker logs -f erpnext-backend"
echo "docker logs -f erpnext-frontend"
echo "docker logs -f erpnext-websocket"
echo ""
echo "# Ver últimas linhas"
echo "docker logs --tail 50 erpnext-backend"
echo ""

# ========== BANCO DE DADOS ==========
echo ""
echo "💾 BANCO DE DADOS (MariaDB)"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "# Acessar MySQL CLI"
echo "docker exec -it erpnext-db mysql -u root -p'ErpN3xt#2026'"
echo ""
echo "# Ver tamanho do banco"
echo "docker exec erpnext-db mysql -u root -p'ErpN3xt#2026' -e 'SELECT table_schema, ROUND(SUM(data_length+index_length)/1024/1024,2) FROM information_schema.tables GROUP BY table_schema;'"
echo ""
echo "# Backup do banco"
echo "docker exec erpnext-db mysqldump -u root -p'ErpN3xt#2026' --all-databases > backup_$(date +%Y%m%d_%H%M%S).sql"
echo ""
echo "# Ver status do MySQL"
echo "docker exec erpnext-db mysqladmin -u root -p'ErpN3xt#2026' status"
echo ""

# ========== REDIS ==========
echo ""
echo "🔴 REDIS CACHE E QUEUE"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "# Testar Redis Cache"
echo "docker exec erpnext-redis-cache redis-cli ping"
echo "# Resposta esperada: PONG"
echo ""
echo "# Testar Redis Queue"
echo "docker exec erpnext-redis-queue redis-cli ping"
echo ""
echo "# Ver info Redis"
echo "docker exec erpnext-redis-cache redis-cli info"
echo ""
echo "# Limpar cache (CUIDADO)"
echo "docker exec erpnext-redis-cache redis-cli FLUSHDB"
echo ""

# ========== FRAPPE/ERPNEXT ==========
echo ""
echo "🚀 FRAPPE/ERPNEXT BENCH COMMANDS"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "# Entrar no container backend"
echo "docker exec -it -u frappe erpnext-backend bash"
echo ""
echo "# Listar sites"
echo "docker exec -u frappe erpnext-backend bench list-sites"
echo ""
echo "# Listar apps instalados"
echo "docker exec -u frappe erpnext-backend bench --site erpnext.local list-apps"
echo ""
echo "# Ver versão de apps"
echo "docker exec -u frappe erpnext-backend bench version"
echo ""
echo "# Executar migrations"
echo "docker exec -u frappe erpnext-backend bench --site erpnext.local migrate"
echo ""
echo "# Rebuild assets"
echo "docker exec -u frappe erpnext-backend bench build --production"
echo ""
echo "# Limpar cache"
echo "docker exec -u frappe erpnext-backend bench --site erpnext.local clear-cache"
echo ""

# ========== USUARIOS E SENHAS ==========
echo ""
echo "🔐 GERENCIAR USUÁRIOS"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "# Redefinir senha de admin"
echo "docker exec -u frappe erpnext-backend bench --site erpnext.local set-admin-password [nova-senha]"
echo ""
echo "# Criar novo usuário"
echo "docker exec -u frappe erpnext-backend bash"
echo "# Depois dentro do container:"
echo "bench --site erpnext.local add-user [email] --password [senha] --first-name [nome]"
echo ""

# ========== PERFORMANCE ==========
echo ""
echo "⚡ PERFORMANCE E DEBUG"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "# Ver uso de recursos"
echo "docker stats --no-stream | grep erpnext"
echo ""
echo "# Inspecionar container"
echo "docker inspect erpnext-backend | jq '.[] | {Name, State, Mounts}'"
echo ""
echo "# Ver variáveis de ambiente"
echo "docker exec erpnext-backend env | grep -i frappe"
echo ""
echo "# Testar conectividade entre containers"
echo "docker exec erpnext-backend curl -v http://redis-cache:6379"
echo ""

# ========== CLEANING ==========
echo ""
echo "🧹 LIMPEZA E MANUTENÇÃO"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "# Ver espaço em disco de volumes"
echo "docker volume ls"
echo "du -sh /var/lib/docker/volumes/*/\_data 2>/dev/null | sort -h"
echo ""
echo "# Limpar logs não usados"
echo "docker container prune --force"
echo ""
echo "# Limpar imagens não usadas"
echo "docker image prune --force"
echo ""
echo "# Limpar volumes não usados (CUIDADO!)"
echo "docker volume prune --force"
echo ""

# ========== TROUBLESHOOTING ==========
echo ""
echo "🆘 TROUBLESHOOTING"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "# Site não sobe - Verificar entrypoint logs"
echo "docker logs erpnext-backend 2>&1 | head -100"
echo ""
echo "# Site não encontrado"
echo "docker exec -u frappe erpnext-backend bash"
echo "ls -la sites/"
echo "bench list-sites"
echo ""
echo "# Erro de conexão com banco"
echo "docker logs erpnext-db"
echo "docker restart erpnext-db"
echo "# Aguarde 30 segundos"
echo ""
echo "# WebSocket não funciona"
echo "docker logs erpnext-websocket"
echo "docker restart erpnext-websocket"
echo ""
echo "# Erro 502 Bad Gateway"
echo "docker logs erpnext-frontend"
echo "docker logs erpnext-backend"
echo ""

# ========== RESTART ==========
echo ""
echo "🔄 REINICIAR SERVIÇOS"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "# Reiniciar container individual"
echo "docker restart erpnext-backend"
echo "docker restart erpnext-frontend"
echo ""
echo "# Reiniciar stack inteiro (Portainer)"
echo "# 1. Vá em Stacks"
echo "# 2. Clique no stack"
echo "# 3. Stop/Start ou Update"
echo ""

echo ""
echo "╔═══════════════════════════════════════════════════════════════╗"
echo "║  Dica: Copie e cole os comandos acima conforme necessário    ║"
echo "║  Use 'docker exec' para executar comandos no container       ║"
echo "║  Use '-u frappe' para rodar como usuário frappe              ║"
echo "╚═══════════════════════════════════════════════════════════════╝"
